const initialState = {
    isEditing: false,
    list: [],
    cardToEdit: {}
}

export const HowToReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}